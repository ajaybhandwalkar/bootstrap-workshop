function getInfo() {
    var data = document.getElementById('data').value;
    if(data=="Celery root"){
        window.alert(data+" is in stock");
    }
    else if(data=="Spaghetti Splash") {
        window.alert(data+" is in stock");
    }
    else if(data=="Killer mushroom") {
        window.alert(data+" is in stock");
    }
    else
        window.alert(data+" is not Available");


}